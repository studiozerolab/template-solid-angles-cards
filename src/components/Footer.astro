---
import site from "../config/site";
---

<footer class="border-t border-slate-200/40">
   <div class="bg-gradient-to-b from-[#1c2738] to-[#0e1014] text-sm text-white font-semibold">
  <div class="mx-auto max-w-6xl px-6 py-12 grid gap-10 md:grid-cols-3">
    <div class="space-y-2">
      <div class="font-extrabold tracking-tight text-xl">{site.name}</div>
      <p class="text-sm text-muted text-slate-300">{site.tagline}</p>
    </div>

    <div class="text-sm space-y-1 text-slate-300">
      <div class="font-semibold mb-1">Contact</div>
      {site.email && (
        <div>
          <span class="text-muted">Email:</span>
          <a class="ml-2 text-primary underline underline-offset-2" href={`mailto:${site.email}`}>{site.email}</a>
        </div>
      )}
      {site.phone && (
        <div>
          <span class="text-muted">Phone:</span>
          <a class="ml-2 text-primary underline underline-offset-2" href={`tel:${site.phone}`}>{site.phone}</a>
        </div>
      )}
      {site.address && <div><span class="text-muted">Address:</span> <span class="ml-2">{site.address}</span></div>}
    </div>

    <div class="text-sm">
      <div class="font-semibold mb-2">Follow</div>
      <div class="flex items-center gap-3">
        {site.socials?.map((s) => (
          <a href={s.href} target="_blank" rel="noopener noreferrer"
             class="inline-flex items-center justify-center w-9 h-9 rounded-md border border-line bg-white/70 backdrop-blur hover:bg-white focus:outline-none focus:ring-2 focus:ring-primary/30"
             aria-label={s.label}>
            {s.icon ?? "↗"}
          </a>
        ))}
      </div>
    </div>
  </div>
  </div>

  <div class="border-t border-white/10 py-2 text-xs text-slate-400 bg-[#0b0c0f]">
    <div class="mx-auto max-w-6xl px-6 py-4 text-xs text-muted flex items-center justify-between">
      <span>© {new Date().getFullYear()} {site.name}. All rights reserved.</span>
      <a href="/" class="hover:text-primary">Made with care by Studio Zero</a>
    </div>
  </div>
</footer>
</div>
